# Corpus documents the PINNED reference build does not reproduce.
#
# `npm run engine:report` renders every corpus document through the
# `@markup-carve/carve` build package.json pins, and compares against the
# committed HTML. A mismatch is not automatically wrong: when a spec rule
# lands here, the engine has not shipped it yet, and the corpus is ahead of
# the pin until someone moves it. That window is normal and the report exists
# to describe it.
#
# What is NOT normal is not knowing which window you are in. carve#533 found
# the pinned build three documents behind with nothing reporting it, because
# `engine:report` was an npm script that no CI job ran.
#
# So the window is DECLARED rather than tolerated. This file lists the slugs
# the pin is knowingly behind on, one per line, each with the reason.
# `npm run engine:report -- --check` fails when the real set differs in
# EITHER direction:
#
#   - a slug mismatches and is not listed  -> new drift, or a corpus fixture
#     that does not match any engine. Red immediately, which is the point.
#   - a slug is listed and now reproduces  -> the pin moved past it. Delete
#     the line in the same commit that moves the pin.
#
# Emptying this file is the normal end state after `npm run bump-carve-pin`.
#
# Format: <slug><space><space><reason>
86-list-lazy-continuation-9  PART 1 S4 folds the flush-left line into the item's open paragraph, because the `:::note` line failed §12's opener test and was absorbed as paragraph text rather than interrupting it (carve#891). ALL THREE ENGINES ANSWER THAT WAY NOW (carve-js#792, carve-rs#718, carve-php#949); what is behind is the PUBLISHED carve-js this repo pins, 0.1.2, which predates the fix. The line clears itself on the next release rather than on any further engine work.
254-colon-fence-separator-must-be-a-space  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-2  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-3  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-4  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-5  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-6  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-7  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-8  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
254-colon-fence-separator-must-be-a-space-9  The corpus states PART 7's colon-fence separator rule (carve#900); the pinned build 52da7be is from 08:09 on the day markup-carve/carve-js#794 narrowed the separator to a space at 12:04, so it still opens the block on a tabbed opener. carve-js main already answers the way this document does. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
255-colon-fence-metadata-slots-must-be-a-space-too  The corpus states PART 7's colon-fence PADDING rule (carve#900); the pinned build 52da7be predates markup-carve/carve-js#798, which reverted the metadata slots back to a space at 13:46 after markup-carve/carve#905 corrected the reading, so it still reads the tabbed metadata slot as an admonition. carve-js main already answers the way this document does; the line clears itself on the next `npm run bump-carve-pin`.
255-colon-fence-metadata-slots-must-be-a-space-too-2  The corpus states PART 7's colon-fence PADDING rule (carve#900); the pinned build 52da7be predates markup-carve/carve-js#798, which reverted the metadata slots back to a space at 13:46 after markup-carve/carve#905 corrected the reading, so it still reads the tabbed metadata slot as an admonition. carve-js main already answers the way this document does; the line clears itself on the next `npm run bump-carve-pin`.
255-colon-fence-metadata-slots-must-be-a-space-too-3  The corpus states PART 7's colon-fence PADDING rule (carve#900); the pinned build 52da7be predates markup-carve/carve-js#798, which reverted the metadata slots back to a space at 13:46 after markup-carve/carve#905 corrected the reading, so it still reads the tabbed metadata slot as an admonition. carve-js main already answers the way this document does; the line clears itself on the next `npm run bump-carve-pin`.
255-colon-fence-metadata-slots-must-be-a-space-too-4  The corpus states PART 7's colon-fence PADDING rule (carve#900); the pinned build 52da7be predates markup-carve/carve-js#798, which reverted the metadata slots back to a space at 13:46 after markup-carve/carve#905 corrected the reading, so it still reads the tabbed metadata slot as an admonition. carve-js main already answers the way this document does; the line clears itself on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-2  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-3  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-4  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-5  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-6  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-7  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-8  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-9  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-10  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-11  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-12  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-13  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-14  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-15  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
256-table-cell-padding-must-be-a-space-16  The corpus states PART 7's table-cell padding rule (carve#904); a tab in a cell's padding slot is ordinary content, so a delimiter cell stops being one and a span marker stops being one. The pinned build accepts the tab, and so do carve-js, carve-php and carve-rs main - measured while writing this, every tab form renders byte-identical to its space form. This is an ENGINE gap rather than pin lag: it clears when the engines narrow the slot, not on the next `npm run bump-carve-pin`.
257-link-and-image-title-slots-must-be-a-space  The corpus states PART 7's link- and image-title padding rule (carve#907); a tab in the slot before a title is not padding, so the inline stays literal text and the tab stays where it was written. carve-rs main already answers exactly this way (markup-carve/carve-rs#729, measured on 378f0d5 - byte-identical to the corpus). carve-js and carve-php still read a title after a tab at every form of the slot, and the PINNED build (52da7be) predates all of it. The line clears when those two ship the rule and the pin moves past it.
257-link-and-image-title-slots-must-be-a-space-2  The corpus states PART 7's link- and image-title padding rule (carve#907); a tab in the slot before a title is not padding, so the inline stays literal text and the tab stays where it was written. carve-rs main already answers exactly this way (markup-carve/carve-rs#729, measured on 378f0d5 - byte-identical to the corpus). carve-js and carve-php still read a title after a tab at every form of the slot, and the PINNED build (52da7be) predates all of it. The line clears when those two ship the rule and the pin moves past it.
257-link-and-image-title-slots-must-be-a-space-3  The corpus states PART 7's link- and image-title padding rule (carve#907); a tab in the slot before a title is not padding, so the inline stays literal text and the tab stays where it was written. carve-rs main already answers exactly this way (markup-carve/carve-rs#729, measured on 378f0d5 - byte-identical to the corpus). carve-js and carve-php still read a title after a tab at every form of the slot, and the PINNED build (52da7be) predates all of it. The line clears when those two ship the rule and the pin moves past it.
257-link-and-image-title-slots-must-be-a-space-4  The corpus states PART 7's link- and image-title padding rule (carve#907); a tab in the slot before a title is not padding, so the inline stays literal text and the tab stays where it was written. carve-rs main already answers exactly this way (markup-carve/carve-rs#729, measured on 378f0d5 - byte-identical to the corpus). carve-js and carve-php still read a title after a tab at every form of the slot, and the PINNED build (52da7be) predates all of it. The line clears when those two ship the rule and the pin moves past it.
257-link-and-image-title-slots-must-be-a-space-5  The corpus states PART 7's link- and image-title padding rule (carve#907); a tab in the slot before a title is not padding, so the inline stays literal text and the tab stays where it was written. carve-rs main already answers exactly this way (markup-carve/carve-rs#729, measured on 378f0d5 - byte-identical to the corpus). carve-js and carve-php still read a title after a tab at every form of the slot, and the PINNED build (52da7be) predates all of it. The line clears when those two ship the rule and the pin moves past it.
257-link-and-image-title-slots-must-be-a-space-6  The corpus states PART 7's link- and image-title padding rule (carve#907); a tab in the slot before a title is not padding, so the inline stays literal text and the tab stays where it was written. carve-rs main already answers exactly this way (markup-carve/carve-rs#729, measured on 378f0d5 - byte-identical to the corpus). carve-js and carve-php still read a title after a tab at every form of the slot, and the PINNED build (52da7be) predates all of it. The line clears when those two ship the rule and the pin moves past it.
258-code-fence-metadata-slots-must-be-a-space-too  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-2  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-3  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-4  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-5  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-6  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-7  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-8  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
258-code-fence-metadata-slots-must-be-a-space-too-9  The corpus states PART 7's code-fence metadata rule (carve#907); a tab in the info-string, "header" or [label] slot is not padding, so the opener is not a fence and the run falls back to an inline verbatim span. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#800, markup-carve/carve-php#955, markup-carve/carve-rs#724) - measured on carve-js 3d95e94, carve-php 876e312 and carve-rs 378f0d5, every one byte-identical to the corpus. What is behind is the PINNED build, 52da7be, which predates the first of them. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
261-a-blank-line-holds-spaces-and-tabs-and-nothing-else  The corpus states PART 1's blank-line rule (carve#890): `blank_line = {whitespace}` over `whitespace = ' ' | '\t'`, so a line holding a zero-width character, a Unicode space separator or a C0 control is CONTENT and the paragraph continues into it. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#802, markup-carve/carve-php#967; carve-rs already did) - measured on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d, all four readings byte-identical to the corpus. What is behind is the PINNED build, 52da7be, whose `isBlank` still used JavaScript's legacy `\s`. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
261-a-blank-line-holds-spaces-and-tabs-and-nothing-else-2  The corpus states PART 1's blank-line rule (carve#890): `blank_line = {whitespace}` over `whitespace = ' ' | '\t'`, so a line holding a zero-width character, a Unicode space separator or a C0 control is CONTENT and the paragraph continues into it. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#802, markup-carve/carve-php#967; carve-rs already did) - measured on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d, all four readings byte-identical to the corpus. What is behind is the PINNED build, 52da7be, whose `isBlank` still used JavaScript's legacy `\s`. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
261-a-blank-line-holds-spaces-and-tabs-and-nothing-else-3  The corpus states PART 1's blank-line rule (carve#890): `blank_line = {whitespace}` over `whitespace = ' ' | '\t'`, so a line holding a zero-width character, a Unicode space separator or a C0 control is CONTENT and the paragraph continues into it. ALL THREE ENGINES ANSWER THAT WAY NOW (markup-carve/carve-js#802, markup-carve/carve-php#967; carve-rs already did) - measured on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d, all four readings byte-identical to the corpus. What is behind is the PINNED build, 52da7be, whose `isBlank` still used JavaScript's legacy `\s`. The line clears itself on the next `npm run bump-carve-pin`, not on any further engine work.
262-a-link-title-takes-exactly-one-space  The corpus states PART 7's padding-slot CARDINALITY rule (carve#912): `link_title = space, ...` is exactly one character, so a second space is not padding and the bracket run is not a link. Unlike the 254-258 lines above this is NOT pin lag: all three engines accept a run at this slot on main - measured under carve#912 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#819, markup-carve/carve-php#972, markup-carve/carve-rs#744), not on a pin bump.
262-a-link-title-takes-exactly-one-space-2  The corpus states PART 7's padding-slot CARDINALITY rule (carve#912) at `image_title = link_title`, one production defined by reference. Unlike the 254-258 lines above this is NOT pin lag: all three engines accept a run at this slot on main - measured under carve#912 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#819, markup-carve/carve-php#972, markup-carve/carve-rs#744), not on a pin bump.
263-a-code-fence-opener-takes-exactly-one-space  The corpus states PART 7's padding-slot CARDINALITY rule (carve#912) at `fenced_code_block = code_fence_open, [space], [code_fence_info]`: a second space reaches `language_info`, matches no shape, and the INVALID-FENCE FALLBACK applies. Unlike the 254-258 lines above this is NOT pin lag: all three engines accept a run at this slot on main - measured under carve#912 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#819, markup-carve/carve-php#972, markup-carve/carve-rs#744), not on a pin bump.
264-a-frontmatter-opener-takes-exactly-one-space  The corpus states PART 7's padding-slot CARDINALITY rule (carve#912) at `frontmatter_open = "---", [space], [frontmatter_format]`: a second space reaches the format token, which is letters and digits only, so the line is not a typed opener. Unlike the 254-258 lines above this is NOT pin lag: all three engines accept a run at this slot on main - measured under carve#912 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#819, markup-carve/carve-php#972, markup-carve/carve-rs#744), not on a pin bump.
265-a-reference-definition-s-metadata-slots-take-exactly-one-space  The corpus states PART 7's padding-slot CARDINALITY rule (carve#912) at the reference definition's `link_title` slot. Unlike the 254-258 lines above this is NOT pin lag: all three engines accept a run at this slot on main - measured under carve#912 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#819, markup-carve/carve-php#972, markup-carve/carve-rs#744), not on a pin bump.
265-a-reference-definition-s-metadata-slots-take-exactly-one-space-2  The corpus states PART 7's padding-slot CARDINALITY rule (carve#912) at the reference definition's `[space, attributes]` slot. Unlike the 254-258 lines above this is NOT pin lag: all three engines accept a run at this slot on main - measured under carve#912 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#819, markup-carve/carve-php#972, markup-carve/carve-rs#744), not on a pin bump.
16-reference-link-5  The corpus USED to pin `[r]: a b c` resolving the label to `a` - a definition with trailing junk - and carve#911 ruled that shape out: the definition is anchored at end of line, so the line is an ordinary paragraph and the reference does not resolve. This is the one pre-existing document the ruling moved, and it is why the ticket's "0 of 690 corpus documents carry it" was wrong. NOT pin lag: all three engines still read it the old way on main (carve-js 68ea9ba, carve-php 1664ee1, carve-rs 2ec3c1c), so it clears with markup-carve/carve-js#821, markup-carve/carve-php#973 and markup-carve/carve-rs#746 rather than on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-2  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-3  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-4  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-5  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-6  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-7  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
266-a-reference-definition-is-anchored-at-end-of-line-8  The corpus states PART 7's reference-definition ANCHOR (carve#911): `reference_definition` ends in `newline`, so what follows the destination and the optional title makes the production fail and the line is an ordinary paragraph. This is NOT pin lag: all three engines read trailing junk as part of a definition on main - measured under carve#911 on carve-js 68ea9ba, carve-php 1664ee1 and carve-rs 2ec3c1c - so the line clears when the engines ship the rule (markup-carve/carve-js#821, markup-carve/carve-php#973, markup-carve/carve-rs#746), not on a pin bump.
267-a-definition-marker-s-separator-is-a-space-and-it-is-a-run-3  The abbreviation form: a no-break space after the separator is content, so the expansion starts with it. The corpus states PART 7's DEFINITION MARKER SEPARATOR rule (carve#892): the separator is a run of ASCII spaces, so the first character that is not one ends the separator and BEGINS the content. NOT pin lag: every engine changes somewhere and no two agree - measured under carve#892 on carve-js b3f49d7, carve-rs 0a613b2 and carve-php c15991f, no engine gives the same answer for the abbreviation and the footnote marker. Clears with markup-carve/carve-js#824, markup-carve/carve-php#977 and markup-carve/carve-rs#748, not on a pin bump.
267-a-definition-marker-s-separator-is-a-space-and-it-is-a-run-4  The footnote form: a no-break space after the separator is content, so the note body starts with it. The executable spec used to refuse this line as a definition ENTIRELY - a fourth answer no engine gave - which is the widest move in this ruling. The corpus states PART 7's DEFINITION MARKER SEPARATOR rule (carve#892): the separator is a run of ASCII spaces, so the first character that is not one ends the separator and BEGINS the content. NOT pin lag: every engine changes somewhere and no two agree - measured under carve#892 on carve-js b3f49d7, carve-rs 0a613b2 and carve-php c15991f, no engine gives the same answer for the abbreviation and the footnote marker. Clears with markup-carve/carve-js#824, markup-carve/carve-php#977 and markup-carve/carve-rs#748, not on a pin bump.
267-a-definition-marker-s-separator-is-a-space-and-it-is-a-run-5  The abbreviation form with a TAB after the run. A tab is not an ASCII space, so it ends the separator and is content; an abbreviation expansion is a raw string, so it survives into the title. The corpus states PART 7's DEFINITION MARKER SEPARATOR rule (carve#892): the separator is a run of ASCII spaces, so the first character that is not one ends the separator and BEGINS the content. NOT pin lag: every engine changes somewhere and no two agree - measured under carve#892 on carve-js b3f49d7, carve-rs 0a613b2 and carve-php c15991f, no engine gives the same answer for the abbreviation and the footnote marker. Clears with markup-carve/carve-js#824, markup-carve/carve-php#977 and markup-carve/carve-rs#748, not on a pin bump.
268-trailing-whitespace-on-a-content-line-is-dropped-2  The SOFT BREAK position, which PART 12 SS7 asserted the opposite of until this ruling. The corpus states PART 2's NO TRAILING WHITESPACE clause (carve#926): a `whitespace` run at the end of a CONTENT LINE is dropped, on every line rather than only a paragraph's last. NOT pin lag: all three engines keep the run on main - measured under carve#926 on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d - so it clears when they ship the rule (markup-carve/carve-js#829, markup-carve/carve-php#980, markup-carve/carve-rs#751), not on a pin bump.
268-trailing-whitespace-on-a-content-line-is-dropped-3  A tab at both the soft-break and the paragraph-final position. The corpus states PART 2's NO TRAILING WHITESPACE clause (carve#926): a `whitespace` run at the end of a CONTENT LINE is dropped, on every line rather than only a paragraph's last. NOT pin lag: all three engines keep the run on main - measured under carve#926 on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d - so it clears when they ship the rule (markup-carve/carve-js#829, markup-carve/carve-php#980, markup-carve/carve-rs#751), not on a pin bump.
268-trailing-whitespace-on-a-content-line-is-dropped-8  The `<SP>U+FEFF<SP>` shape the ticket was raised on: the BOM is content and the trailing SPACE is dropped. The corpus states PART 2's NO TRAILING WHITESPACE clause (carve#926): a `whitespace` run at the end of a CONTENT LINE is dropped, on every line rather than only a paragraph's last. NOT pin lag: all three engines keep the run on main - measured under carve#926 on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d - so it clears when they ship the rule (markup-carve/carve-js#829, markup-carve/carve-php#980, markup-carve/carve-rs#751), not on a pin bump.
268-trailing-whitespace-on-a-content-line-is-dropped-12  The line block, where SS23's MEDIAL GAPS converts a run of two or more columns into NBSP CONTENT first, so only the one-column trailing run is left for this rule to drop. The corpus states PART 2's NO TRAILING WHITESPACE clause (carve#926): a `whitespace` run at the end of a CONTENT LINE is dropped, on every line rather than only a paragraph's last. NOT pin lag: all three engines keep the run on main - measured under carve#926 on carve-js 6647523, carve-php 3de1184 and carve-rs fcb879d - so it clears when they ship the rule (markup-carve/carve-js#829, markup-carve/carve-php#980, markup-carve/carve-rs#751), not on a pin bump.
269-a-definition-body-continuation-indented-past-its-column-is-lazy-text  The corpus states PART 9's definition-body continuation rule (carve#918): a line indented PAST the body's column continues the open paragraph, so its `>` is a greater-than sign. The published build reads the extra indentation as opening a nested block quote; markup-carve/carve-js#822 and markup-carve/carve-php#974 track the engine work. The two CONTROL documents beside it reproduce under the pin; only this one is ahead. The line clears when carve-js ships the rule and the pin moves past it.
271-the-flush-left-line-after-a-container-a-quoted-line-opened  The corpus states PART 1 S4 (carve#920): the div a quoted line opened is EMPTY, so no container in the stack holds an open paragraph and `tail` closes the quote. The pinned carve-js folds `tail` into the div. It already answers the LIST-ITEM twin the corpus way (270-a-real-div-in-a-container reproduces), so the divergence is the container kind rather than the rule; tracked as markup-carve/carve-js#833 and markup-carve/carve-php#982.
271-the-flush-left-line-after-a-container-a-quoted-line-opened-3  The same clause with the div CLOSED and holding a body: a closed container holds no open paragraph either, so `tail` is top-level. The pinned carve-js keeps it inside the quote while putting the identical list-item shape at top level (the CONTROL under 270-a-real-div-in-a-container); tracked as markup-carve/carve-js#833 and markup-carve/carve-php#982.
272-an-autolink-body-admits-non-ascii-and-excludes-format-characters-6  The corpus states PART 3's autolink-body rule (carve#844): U+200B ZERO WIDTH SPACE is a FORMAT character, so it is not a `url_char` and the line is literal text. The pinned carve-js opens an autolink on it - JavaScript's `\s` holds U+FEFF and not U+200B, so the neighbouring BOM document reproduces for the wrong reason while this one does not reproduce at all. NOT pin lag: carve-js main 0c71c7d behaves the same, so it clears when the engine ships the rule (markup-carve/carve-js#834), not on a pin bump. The other nine documents of the category reproduce under the pin.
252-a-tab-separates-two-attributes-and-pads-a-block-as-a-space-does  The corpus states PART 4's THE INLINE INTERIOR IS SPACE-ONLY clause (carve#906): a tab does not pad or separate an INLINE attribute block, so the braces show. This document previously pinned the opposite and is rewritten rather than deleted. NOT pin lag: all three engines read the tab as a separator on their own mains - measured under carve#906 on carve-js 0c71c7d, carve-php d993758 and carve-rs 2d5de72 - so it clears when they ship the rule (markup-carve/carve-js#836, markup-carve/carve-php#985, markup-carve/carve-rs#757), not on a pin bump.
252-a-tab-separates-two-attributes-and-pads-a-block-as-a-space-does-2  The same clause at the boundary after an UNQUOTED value: a tab ends the value and then satisfies no separator, so the block fails. The QUOTED half of the document does not move and is the control inside it. Same three engine tickets; same reason it is not pin lag.
252-a-tab-separates-two-attributes-and-pads-a-block-as-a-space-does-3  The same clause at the blessed EMPTY block, which is a separate position rather than a use of the separator - narrowing the separator alone leaves this document green, which is why the executable spec needed two edits. Same three engine tickets; same reason it is not pin lag.
274-a-quoted-attribute-value-stops-at-the-newline  The corpus states PART 4's A QUOTED VALUE STOPS AT THE NEWLINE clause (carve#888): a line break inside a quoted attribute value ends the production, so the INLINE block is unrecognized. The pinned carve-js reads it as an attribute whose value carries the newline - the one way an inline block could still span lines after carve#897 and carve#906. NOT pin lag: carve-js main 0c71c7d does the same, so it clears when the engine ships the rule (markup-carve/carve-js#838), not on a pin bump. carve-php and carve-rs already answer the inline form the corpus way.
274-a-quoted-attribute-value-stops-at-the-newline-3  The same production read by the BLOCK-attribute line, where all three engines are behind and do not agree with each other: carve-js keeps the newline in the value, carve-php and carve-rs collapse it to a space, which no production describes in either normative file. Measured on carve-js 0c71c7d, carve-php d993758 and carve-rs 2d5de72; markup-carve/carve-js#838, markup-carve/carve-php#986 and markup-carve/carve-rs#758 carry it.
