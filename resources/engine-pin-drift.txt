# Corpus documents the PINNED reference build does not reproduce.
#
# `npm run engine:report` renders every corpus document through the
# `@markup-carve/carve` build package.json pins, and compares against the
# committed HTML. A mismatch is not automatically wrong: when a spec rule
# lands here, the engine has not shipped it yet, and the corpus is ahead of
# the pin until someone moves it. That window is normal and the report exists
# to describe it.
#
# What is NOT normal is not knowing which window you are in. carve#533 found
# the pinned build three documents behind with nothing reporting it, because
# `engine:report` was an npm script that no CI job ran.
#
# So the window is DECLARED rather than tolerated. This file lists the slugs
# the pin is knowingly behind on, one per line, each with the reason.
# `npm run engine:report -- --check` fails when the real set differs in
# EITHER direction:
#
#   - a slug mismatches and is not listed  -> new drift, or a corpus fixture
#     that does not match any engine. Red immediately, which is the point.
#   - a slug is listed and now reproduces  -> the pin moved past it. Delete
#     the line in the same commit that moves the pin.
#
# Emptying this file is the normal end state after `npm run bump-carve-pin`.
#
# Format: <slug><space><space><reason>
344-a-comment-only-line-in-a-line-block-is-removed-before-any-inline-run  carve#1333: the pin removes a comment-only verse line at the INLINE layer, so an unclosed verbatim run opened on an earlier line claims it first and the comment text is published inside the code span. PART 9 section 23 now says the removal is a block-layer step. The no-run control beside this case (-2) already reproduces, which is what makes the pair discriminating.
347-a-comment-fence-reached-through-a-quote-registers-nothing-either  markup-carve/carve-js#1177: the pin registers a link reference definition written inside a QUOTED comment fence, so the use below resolves instead of staying literal. carve#1309 ruled the definition is not registered wherever the fence sits, and this is the third column it can sit at. The FOOTNOTE kind beside this case (-2) already reproduces, which is what makes the pair discriminating: the leak sorts definitions by kind, so a single-kind fixture would let this build pass on the half it gets right. The no-fence control (-3) reproduces too.
348-a-closed-inline-construct-spanning-a-verse-boundary  carve#1351: the pin leaves the boundary a BARE NEWLINE where a closed inline construct spans it, so an emphasized couplet loses the per-line layout the block exists to preserve. PART 9 section 23's AN INLINE CONSTRUCT IS NOT A DEPTH THE RULE STOPS AT is the fix: the subject is the soft break NODE, and the construct encloses one rather than consuming it.
348-a-closed-inline-construct-spanning-a-verse-boundary-3  carve#1351: the same rule where the construct closes before a further line, so the pin hardens the second boundary of one stanza and not the first. The verbatim control (-2) and the backslash control (-4) both reproduce, which is what makes the set discriminating: the pin gets the two shapes that leave no soft break right and only the shape that leaves one wrong.
348-a-closed-inline-construct-spanning-a-verse-boundary-5  carve#1351: the same rule with a preserved leading run on the second line, which is the shape an implementation that re-reads the construct's raw text loses the NBSPs on as well.
349-a-container-whose-table-ends-on-a-continuation-row  carve#1348: the pin folds the flush-left line into the ITEM where the item's table ends on a continuation row, while closing the item when the same table ends on a standard row. PART 9 section 5 T6's IT LEAVES NO PARAGRAPH OPEN, LIKE THE ROW IT APPENDS TO is the fix. The standard-row control (-2) reproduces, which is what makes the pair discriminating.
349-a-container-whose-table-ends-on-a-continuation-row-3  carve#1348: the QUOTE half, which no implementation had right - the pin keeps the flush-left line inside the quote here and sends it out of the same quote ending on a standard row.
349-a-container-whose-table-ends-on-a-continuation-row-5  carve#1348: the same quote one container in. The pin peels the quote marker off ONE line, so the run above it is lost and the answer differs from the bare spelling, which section 5 T6's A QUOTE IS ASKED ITS OWN BODY refuses. The quote's own standard-row control (-4) reproduces, which is what makes the set discriminating.
349-a-container-whose-table-ends-on-a-continuation-row-6  carve#1348: the definition-body spelling of the first case.
350-a-definition-at-a-container-s-content-column  carve#1350: the pin folds the flush-left line into the ITEM and does not register the definition, so a reference at the item's content column resolves nowhere. PART 1 S4's A DEFINITION AT THE CONTENT COLUMN ENDS THE PARAGRAPH, NOT THE CONTAINER is the fix, and section 10 I5 is what it rests on. The below-column control (-3) and the abbreviation control (-4) both reproduce, which is what makes the set discriminating: the pin gets the two columns where the line is NOT a definition right and only the one where it is wrong.
350-a-definition-at-a-container-s-content-column-2  carve#1350: the FOOTNOTE kind of the same case. Kept beside the link kind because an engine that sorts definitions by kind can pass on the half it gets right.
350-a-definition-at-a-container-s-content-column-5  carve#1350: the definition-body spelling, which is the case carve-php already answers and the pin does not.
350-a-definition-at-a-container-s-content-column-6  carve#1350: the COMMENT at a definition body's content column, which section 10 I5 lists alongside the definitions. All three engines fold here.
353-a-bracketed-construct-spanning-a-verse-boundary  carve#1351: the coverage that ruling deferred, so the pin fails it for exactly the reason it fails 348 - a LINK LABEL spanning a stanza boundary encloses a soft break, and the pin leaves that boundary a bare newline. The parse itself already reproduces: every 351 document, the same label in an ordinary paragraph, matches.
353-a-bracketed-construct-spanning-a-verse-boundary-2  carve#1351: the SEMANTIC SPAN spelling of the same node, kept beside the link because the two reach the rule through different constructs and an engine can fix one.
353-a-bracketed-construct-spanning-a-verse-boundary-3  carve#1351: the BRACED spelling, which is what makes the set about the node rather than about a list of enclosures.
355-a-container-whose-table-ends-on-a-joined-header-row  carve#1348: the pin folds the flush-left line into the ITEM where the item's table ends on a continuation row, which is the same drift already declared for 349 - carve#1354 only added the rows the reader used to refuse, so the container half is unchanged and the pin fails it for the reason it already fails the other spelling. The 354 documents, which are the reader half alone, all reproduce.
355-a-container-whose-table-ends-on-a-joined-header-row-2  carve#1348: the all-empty continuation spelling of the same container.
355-a-container-whose-table-ends-on-a-joined-header-row-3  carve#1348: the QUOTE spelling, which is the half of that ruling no implementation had right.
