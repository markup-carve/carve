# Corpus documents the PINNED reference build does not reproduce.
#
# `npm run engine:report` renders every corpus document through the
# `@markup-carve/carve` build package.json pins, and compares against the
# committed HTML. A mismatch is not automatically wrong: when a spec rule
# lands here, the engine has not shipped it yet, and the corpus is ahead of
# the pin until someone moves it. That window is normal and the report exists
# to describe it.
#
# What is NOT normal is not knowing which window you are in. carve#533 found
# the pinned build three documents behind with nothing reporting it, because
# `engine:report` was an npm script that no CI job ran.
#
# So the window is DECLARED rather than tolerated. This file lists the slugs
# the pin is knowingly behind on, one per line, each with the reason.
# `npm run engine:report -- --check` fails when the real set differs in
# EITHER direction:
#
#   - a slug mismatches and is not listed  -> new drift, or a corpus fixture
#     that does not match any engine. Red immediately, which is the point.
#   - a slug is listed and now reproduces  -> the pin moved past it. Delete
#     the line in the same commit that moves the pin.
#
# Emptying this file is the normal end state after `npm run bump-carve-pin`.
#
# Format: <slug><space><space><reason>

# PART 10 §T9: every `th` carries a `scope`. The pinned build emits none, so
# every document with a header cell differs by exactly that attribute and by
# nothing else. Clears in one bump once the engines ship the rule.
09-tables  A header cell now carries `scope`; the pin emits none.
09-tables-2  A header cell now carries `scope`; the pin emits none.
09-tables-3  A header cell now carries `scope`; the pin emits none.
09-tables-4  A header cell now carries `scope`; the pin emits none.
09-tables-5  A header cell now carries `scope`; the pin emits none.
09-tables-6  A header cell now carries `scope`; the pin emits none.
09-tables-7  A header cell now carries `scope`; the pin emits none.
09-tables-8  A header cell now carries `scope`; the pin emits none.
10-tables-with-rowspan-and-colspan  A header cell now carries `scope`; the pin emits none.
100-table-row-attributes  A header cell now carries `scope`; the pin emits none.
101-table-header-cell-rowspan  A header cell now carries `scope`; the pin emits none.
106-blocked-span-marker-renders-as-empty-cell  A header cell now carries `scope`; the pin emits none.
107-colspan-marker-scans-left-past-a-consumed-cell  A header cell now carries `scope`; the pin emits none.
115-a-continuation-row-needs-a-body-row  A header cell now carries `scope`; the pin emits none.
146-table-as-a-block-opener-in-a-list-item  A header cell now carries `scope`; the pin emits none.
203-a-footnote-body-holds-blocks-and-they-render-where-they-were-written  A header cell now carries `scope`; the pin emits none.
225-a-footnote-body-s-last-block-when-it-is-not-a-paragraph-gets-a-synthesized-paragraph-for-the-backlink-3  A header cell now carries `scope`; the pin emits none.
256-table-cell-padding-must-be-a-space-18  A header cell now carries `scope`; the pin emits none.
256-table-cell-padding-must-be-a-space-19  A header cell now carries `scope`; the pin emits none.
256-table-cell-padding-must-be-a-space-6  A header cell now carries `scope`; the pin emits none.
256-table-cell-padding-must-be-a-space-7  A header cell now carries `scope`; the pin emits none.
256-table-cell-padding-must-be-a-space-8  A header cell now carries `scope`; the pin emits none.
256-table-cell-padding-must-be-a-space-9  A header cell now carries `scope`; the pin emits none.
281-a-caption-attaches-across-one-blank-line  A header cell now carries `scope`; the pin emits none.
281-a-caption-attaches-across-one-blank-line-2  A header cell now carries `scope`; the pin emits none.
282-two-blank-lines-detach-a-caption  A header cell now carries `scope`; the pin emits none.
282-two-blank-lines-detach-a-caption-2  A header cell now carries `scope`; the pin emits none.
284-a-ragged-table-keeps-each-row-s-cell-count-2  A header cell now carries `scope`; the pin emits none.
284-a-ragged-table-keeps-each-row-s-cell-count-3  A header cell now carries `scope`; the pin emits none.
285-adjacent-block-openers-in-an-attached-run-stay-separate-2  A header cell now carries `scope`; the pin emits none.
47-numbered-cross-references-4  A header cell now carries `scope`; the pin emits none.
48-table-column-alignment  A header cell now carries `scope`; the pin emits none.
49-table-per-cell-alignment-override  A header cell now carries `scope`; the pin emits none.
51-table-without-alignment  A header cell now carries `scope`; the pin emits none.
52-table-alignment-with-colspan  A header cell now carries `scope`; the pin emits none.
53-table-doubled-alignment-marker  A header cell now carries `scope`; the pin emits none.
56-table-cell-escaped-pipe  A header cell now carries `scope`; the pin emits none.
57-table-cell-pipe-inside-code-span  A header cell now carries `scope`; the pin emits none.
61-table-stacked-rowspan  A header cell now carries `scope`; the pin emits none.
63-table-multi-line-cell-continuation  A header cell now carries `scope`; the pin emits none.
64-table-rowspan-with-multi-line-content  A header cell now carries `scope`; the pin emits none.
89-block-attribute-lines-4  A header cell now carries `scope`; the pin emits none.
98-table-span-marker-in-first-column  A header cell now carries `scope`; the pin emits none.
99-table-cell-attributes  A header cell now carries `scope`; the pin emits none.
293-a-semantic-name-renames-the-span-and-the-leftovers-ride-the-element-3  PART 9 §9's riding rule: `[x]{kbd onclick="steal()"}` is `<kbd>x</kbd>` now, where the pin still wraps the consumed name in the span the author's stripped attribute left behind.
45-inline-extensions  The `:name[…]` spelling has no core handler with the tier split, so `:kbd[Ctrl+C]` takes the generic `ext-kbd` fallback here. The pin still renders `<kbd>`; it clears when the SemanticSpan extension lands in the engines and the pin moves.
45-inline-extensions-9  The riding rule again, on the case that also carries a hostile attribute: the id and class land on the `<kbd>` rather than on an outer span.
45-inline-extensions-10  `samp`, `var`, `cite` and `dfn` are the extension's now, so a core render leaves them as ordinary attributes where the pin consumes them.
71-attribute-edge-cases-11  The riding rule on an authored id and class (`[x]{#k .key kbd}` -> `<kbd id="k" class="key">`).
301-a-derived-title-yields-to-an-authored-one  PART 9 §9: a derived `title`/`datetime` occupies the same slot as an authored one, so the authored value wins and the element carries the attribute once. The pin emits both, which is the defect the rule closes; it clears with the engine work and a pin bump.

# PART 9 §4a: a captioned quote is a quote carrying an attribution, not a
# figure. The pinned build still wraps it in `<figure>`/`<figcaption>`.
# Clears with the engine fixes for carve#1159 and a pin bump.
05-lists-20  A captioned quote renders `<blockquote><footer>`; the pin renders `<figure><figcaption>`.
07-blockquote-with-attribution  A captioned quote renders `<blockquote><footer>`; the pin renders `<figure><figcaption>`.
55-blockquote-caption-after-a-blank-line  A captioned quote renders `<blockquote><footer>`; the pin renders `<figure><figcaption>`.
282-two-blank-lines-detach-a-caption-5  A captioned quote renders `<blockquote><footer>`; the pin renders `<figure><figcaption>`.
