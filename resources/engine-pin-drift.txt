# Corpus documents the PINNED reference build does not reproduce.
#
# `npm run engine:report` renders every corpus document through the
# `@markup-carve/carve` build package.json pins, and compares against the
# committed HTML. A mismatch is not automatically wrong: when a spec rule
# lands here, the engine has not shipped it yet, and the corpus is ahead of
# the pin until someone moves it. That window is normal and the report exists
# to describe it.
#
# What is NOT normal is not knowing which window you are in. carve#533 found
# the pinned build three documents behind with nothing reporting it, because
# `engine:report` was an npm script that no CI job ran.
#
# So the window is DECLARED rather than tolerated. This file lists the slugs
# the pin is knowingly behind on, one per line, each with the reason.
# `npm run engine:report -- --check` fails when the real set differs in
# EITHER direction:
#
#   - a slug mismatches and is not listed  -> new drift, or a corpus fixture
#     that does not match any engine. Red immediately, which is the point.
#   - a slug is listed and now reproduces  -> the pin moved past it. Delete
#     the line in the same commit that moves the pin.
#
# Emptying this file is the normal end state after `npm run bump-carve-pin`.
#
# Format: <slug><space><space><reason>
430-below-a-definition-body-s-column-an-invisible-line-folds-as-text  the below-column band ruling (carve#1800): the pin publishes a link or footnote definition below a definition description's content column as document-level text, and discards an attribute line there, where both now fold into the description (markup-carve/carve-js#1550)
430-below-a-definition-body-s-column-an-invisible-line-folds-as-text-2  the below-column band ruling (carve#1800): the pin publishes a link or footnote definition below a definition description's content column as document-level text, and discards an attribute line there, where both now fold into the description (markup-carve/carve-js#1550)
430-below-a-definition-body-s-column-an-invisible-line-folds-as-text-3  the below-column band ruling (carve#1800): the pin publishes a link or footnote definition below a definition description's content column as document-level text, and discards an attribute line there, where both now fold into the description (markup-carve/carve-js#1550)
430-below-a-definition-body-s-column-an-invisible-line-folds-as-text-4  the below-column band ruling (carve#1800): the pin publishes a link or footnote definition below a definition description's content column as document-level text, and discards an attribute line there, where both now fold into the description (markup-carve/carve-js#1550)
430-below-a-definition-body-s-column-an-invisible-line-folds-as-text-6  the below-column band ruling (carve#1800): the pin publishes a link or footnote definition below a definition description's content column as document-level text, and discards an attribute line there, where both now fold into the description (markup-carve/carve-js#1550)
433-a-comment-in-a-footnote-body-is-invisible-in-both-spellings  the classification's property 2 (carve#1783, SS10 I5 AN INVISIBLE LINE IS ONE CLASSIFICATION): the pin leaves a blank line in a footnote body where the comment was authored, so the invisible line emits residual whitespace at its authored location
433-a-comment-in-a-footnote-body-is-invisible-in-both-spellings-2  the classification's property 2 (carve#1783, SS10 I5 AN INVISIBLE LINE IS ONE CLASSIFICATION): the pin leaves a blank line in a footnote body where the comment was authored, so the invisible line emits residual whitespace at its authored location
434-an-unresolved-image-gives-its-whole-caption-slot-back-at-any-depth-4  a reference image inside a container promotes and DROPS its caption in the pin, where the inline form in the same list item and the reference form at top level both keep it (markup-carve/carve-js#1553). PART 9 §4 makes the image paragraph a captionable host on its inline content, with nothing said about the container, so the caption attaches wherever the block sits - which is what PART 9R R7's single promotion phase makes unreachable.
435-the-continuation-marker-s-column-gate-reaches-every-container-11  the column gate ruling (carve#1814, SS17 L3 AND FLUSH-LEFT MEANS COLUMN 0): the pin attaches a `+`-marked block that begins below document column 0 in a footnote body, a definition description and a block quote, where the marker now reaches a flush-left block and nothing else
435-the-continuation-marker-s-column-gate-reaches-every-container-13  the column gate ruling (carve#1814, SS17 L3 AND FLUSH-LEFT MEANS COLUMN 0): the pin attaches a `+`-marked block that begins below document column 0 in a footnote body, a definition description and a block quote, where the marker now reaches a flush-left block and nothing else
435-the-continuation-marker-s-column-gate-reaches-every-container-2  the column gate ruling (carve#1814, SS17 L3 AND FLUSH-LEFT MEANS COLUMN 0): the pin attaches a `+`-marked block that begins below document column 0 in a footnote body, a definition description and a block quote, where the marker now reaches a flush-left block and nothing else
435-the-continuation-marker-s-column-gate-reaches-every-container-4  the column gate ruling (carve#1814, SS17 L3 AND FLUSH-LEFT MEANS COLUMN 0): the pin attaches a `+`-marked block that begins below document column 0 in a footnote body, a definition description and a block quote, where the marker now reaches a flush-left block and nothing else
435-the-continuation-marker-s-column-gate-reaches-every-container-7  the column gate ruling (carve#1814, SS17 L3 AND FLUSH-LEFT MEANS COLUMN 0): the pin attaches a `+`-marked block that begins below document column 0 in a footnote body, a definition description and a block quote, where the marker now reaches a flush-left block and nothing else
