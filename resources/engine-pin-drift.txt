# Corpus documents the PINNED reference build does not reproduce.
#
# `npm run engine:report` renders every corpus document through the
# `@markup-carve/carve` build package.json pins, and compares against the
# committed HTML. A mismatch is not automatically wrong: when a spec rule
# lands here, the engine has not shipped it yet, and the corpus is ahead of
# the pin until someone moves it. That window is normal and the report exists
# to describe it.
#
# What is NOT normal is not knowing which window you are in. carve#533 found
# the pinned build three documents behind with nothing reporting it, because
# `engine:report` was an npm script that no CI job ran.
#
# So the window is DECLARED rather than tolerated. This file lists the slugs
# the pin is knowingly behind on, one per line, each with the reason.
# `npm run engine:report -- --check` fails when the real set differs in
# EITHER direction:
#
#   - a slug mismatches and is not listed  -> new drift, or a corpus fixture
#     that does not match any engine. Red immediately, which is the point.
#   - a slug is listed and now reproduces  -> the pin moved past it. Delete
#     the line in the same commit that moves the pin.
#
# Emptying this file is the normal end state after `npm run bump-carve-pin`.
#
# Format: <slug><space><space><reason>

294-a-language-attribute-is-exact-sugar-for-lang  The `{:TAG}` production this change adds (carve#1114). The pinned build has no rule for it, so the block stays literal source and the span never gets its `lang`. Clears with markup-carve/carve-js#1005 and a pin bump; there is no engine work left after that.
294-a-language-attribute-is-exact-sugar-for-lang-2  Same rule, script/region/private-use/grandfathered tags.
294-a-language-attribute-is-exact-sugar-for-lang-3  Same rule, the empty form `{:}` and its `lang=""`.
294-a-language-attribute-is-exact-sugar-for-lang-4  Same rule, mixed with the other attribute kinds.
294-a-language-attribute-is-exact-sugar-for-lang-5  Same rule, on a block-attribute line.
295-a-malformed-language-tag-leaves-the-whole-block-literal-2  The eight-character subtag cap. The pin agrees on the NINE-character half of this case (literal either way, which is why the other three examples in this category are not declared) and disagrees on the eight-character half, where the tag is valid and the pin still has no production for it.
297-the-language-sigil-takes-no-padding  `{: fr}` is the empty language attribute plus a separate boolean `fr`, and the control `{:fr}` beside it is the ordinary form. The pin has no language production, so it reads both blocks as literal source instead.
297-the-language-sigil-takes-no-padding-2  The control beside it: the ordinary `{:fr}` form, literal under the pin for the same missing production.
296-a-language-attribute-and-lang-are-one-key  The collision rule. `{:fr lang=de}` needs the shorthand to desugar before the merge sees it; without the production the pin leaves the whole block literal, so there is nothing to merge.
296-a-language-attribute-and-lang-are-one-key-2  Same, with the surviving value in the slot the shorthand opened.
296-a-language-attribute-and-lang-are-one-key-3  Same, two shorthands colliding.
296-a-language-attribute-and-lang-are-one-key-4  Same, across accumulated block-attribute lines.
