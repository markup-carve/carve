# Corpus documents the PINNED reference build does not reproduce.
#
# `npm run engine:report` renders every corpus document through the
# `@markup-carve/carve` build package.json pins, and compares against the
# committed HTML. A mismatch is not automatically wrong: when a spec rule
# lands here, the engine has not shipped it yet, and the corpus is ahead of
# the pin until someone moves it. That window is normal and the report exists
# to describe it.
#
# What is NOT normal is not knowing which window you are in. carve#533 found
# the pinned build three documents behind with nothing reporting it, because
# `engine:report` was an npm script that no CI job ran.
#
# So the window is DECLARED rather than tolerated. This file lists the slugs
# the pin is knowingly behind on, one per line, each with the reason.
# `npm run engine:report -- --check` fails when the real set differs in
# EITHER direction:
#
#   - a slug mismatches and is not listed  -> new drift, or a corpus fixture
#     that does not match any engine. Red immediately, which is the point.
#   - a slug is listed and now reproduces  -> the pin moved past it. Delete
#     the line in the same commit that moves the pin.
#
# Emptying this file is the normal end state after `npm run bump-carve-pin`.
#
# Format: <slug><space><space><reason>
439-a-colon-followed-by-only-whitespace-is-not-a-description-7  carve#1836 rules that a separator space then a lone tab opens no description; the pinned build still opens an empty one. Clears when the pin moves past the ports (carve-js#1564, carve-php#1814, carve-rs#1456).
442-a-marker-folds-only-strictly-between-the-item-s-base-and-content-column-3  carve#1906 pins the marker fold window: a marker at the item's content column nests, so its definition is metadata. The pinned build reads the window as a fixed column. Fixed on carve-js main (carve-js#1598, carve-js#1601); clears when the pin moves past them.
442-a-marker-folds-only-strictly-between-the-item-s-base-and-content-column-4  carve#1906 pins the marker fold window: a sibling marker at the base column opens a real item, so its definition is metadata. The pinned build reads the window as a fixed column. Fixed on carve-js main (carve-js#1598, carve-js#1601); clears when the pin moves past them.
442-a-marker-folds-only-strictly-between-the-item-s-base-and-content-column-5  carve#1906 pins the marker fold window: column 2 is inside a four-column item's window, so the line is lead text. The pinned build reads the window as a fixed column. Fixed on carve-js main (carve-js#1598, carve-js#1601); clears when the pin moves past them.
442-a-marker-folds-only-strictly-between-the-item-s-base-and-content-column-7  carve#1906 pins the marker fold window: a marker at the content column nests, so its definition is metadata. The pinned build reads the window as a fixed column. Fixed on carve-js main (carve-js#1598, carve-js#1601); clears when the pin moves past them.
442-a-marker-folds-only-strictly-between-the-item-s-base-and-content-column-8  carve#1906 pins the marker fold window: an ordered marker at the base column opens its own list, so its definition is metadata. The pinned build reads the window as a fixed column. Fixed on carve-js main (carve-js#1598, carve-js#1601); clears when the pin moves past them.
442-a-marker-folds-only-strictly-between-the-item-s-base-and-content-column-9  carve#1906 pins the marker fold window: a folded marker does not become the owner the next line is measured against. The pinned build reads the window as a fixed column. Fixed on carve-js main (carve-js#1598, carve-js#1601); clears when the pin moves past them.
443-an-unterminated-comment-fence-in-a-list-item-is-the-line-form  carve#1909 pins that an unterminated `%%%` at an item's content column is one `%%` line comment, so the column-0 follower is a document paragraph. The pinned build gives the degraded fence a third answer and leaves the item's paragraph open. Fixed on carve-js main (carve-js#1602); clears when the pin moves past it.
443-an-unterminated-comment-fence-in-a-list-item-is-the-line-form-5  carve#1909 pins that the band does not end at the content column: a fence written past it degrades the same way. The pinned build gives the degraded fence a third answer and leaves the item's paragraph open. Fixed on carve-js main (carve-js#1602); clears when the pin moves past it.
443-an-unterminated-comment-fence-in-a-list-item-is-the-line-form-6  carve#1909 pins that the band moves with the content column: an ordered marker hands out at column 3. The pinned build gives the degraded fence a third answer and leaves the item's paragraph open. Fixed on carve-js main (carve-js#1602); clears when the pin moves past it.
443-an-unterminated-comment-fence-in-a-list-item-is-the-line-form-7  carve#1909 pins that the band moves with the content column: a padded marker hands out at column 4. The pinned build gives the degraded fence a third answer and leaves the item's paragraph open. Fixed on carve-js main (carve-js#1602); clears when the pin moves past it.
443-an-unterminated-comment-fence-in-a-list-item-is-the-line-form-8  carve#1909 pins the closer's scope: a run written after the item has ended closes nothing, so both followers are published. The pinned build gives the degraded fence a third answer and leaves the item's paragraph open. Fixed on carve-js main (carve-js#1602); clears when the pin moves past it.
443-an-unterminated-comment-fence-in-a-list-item-is-the-line-form-9  carve#1909 pins §28's exact-length closer match: a wider run below the opener is not a closer. The pinned build gives the degraded fence a third answer and leaves the item's paragraph open. Fixed on carve-js main (carve-js#1602); clears when the pin moves past it.
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-3  carve#1911 pins that a heading one past the column is a visible opener (§10 I1), so `tail` is a document paragraph. The pinned build keeps `tail` in the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-5  carve#1911 pins that a thematic break one past the column closes the paragraph the same way. The pinned build keeps `tail` in the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-6  carve#1911 pins that a table row one past the column closes it too, and a flush-left line is not the table's next row. The pinned build keeps `tail` in the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-7  carve#1911 pins that an attribute block one past the column closes the paragraph (§10 I5) and the ended body leaves it no target. The pinned build keeps `tail` in the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-12  carve#1911 pins that the band moves with the separator width: a four-space separator puts "past" at column 6. The pinned build keeps `tail` in the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-13  carve#1911 pins that the surviving context is the ITEM, so `tail` is the item's own text. The pinned build keeps `tail` in the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
444-an-opener-at-or-past-a-description-body-s-column-closes-its-paragraph-14  carve#1911 pins that the line below the ended body is classified in the surviving context: a definition one column in opens nothing at document level and is paragraph text. The pinned build folds it back into the `dd`. Not landed in any engine; clears when the pin moves past the ports (carve-js#1604, carve-php#1874, carve-rs#1525).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item  carve#1903 pins that an unterminated `%%%` at the item frame's column 0 is one `%%` line comment, so the follower stays in the item. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item-4  carve#1903 pins that the band is not the bullet's: an ordered marker answers the same at its frame's column 0. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item-5  carve#1903 pins that the band does not move with the CONTENT column: a padded marker still degrades at column 0. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item-6  carve#1903 pins that every container's own column 0, at every depth: the inner item keeps its follower. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item-7  carve#1903 pins that a wider unterminated run degrades the same - length is not what decides it. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item-8  carve#1903 pins that §28 matches a closer on EXACT length, so a wider run below the opener leaves the opener degraded. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
445-a-degraded-comment-fence-at-a-container-s-column-0-keeps-the-follower-in-the-item-10  carve#1903 pins that the degraded comment leaves the LIST open too, so the sibling marker is the same list's second item. The pinned build ends the item there. Not landed in any engine; clears when the pin moves past the ports (carve-js#1607, carve-php#1877, carve-rs#1529).
447-the-host-does-not-change-which-column-a-definition-reaches-10  carve#1918 pins that a definition between a list item's content column and a note body's reaches the ITEM. The pinned build registers it and publishes its characters as well. Not landed on carve-js main (`3ca6d8c`) either; clears when the pin moves past the ports (carve-js#1610, carve-php#1879, carve-rs#1532).
447-the-host-does-not-change-which-column-a-definition-reaches-11  carve#1918 pins the same band with a footnote definition in it. The pinned build neither registers it nor keeps it invisible. Not landed on carve-js main (`3ca6d8c`) either; clears when the pin moves past the ports (carve-js#1610, carve-php#1879, carve-rs#1532).
447-the-host-does-not-change-which-column-a-definition-reaches-13  carve#1918 pins the band with a description body outside and a note body inside. The pinned build registers the definition and publishes it in the `dd`. Not landed on carve-js main (`3ca6d8c`) either; clears when the pin moves past the ports (carve-js#1610, carve-php#1879, carve-rs#1532).
447-the-host-does-not-change-which-column-a-definition-reaches-14  carve#1918 pins the band with a note inside a note. The pinned build publishes the footnote definition as a paragraph in the outer note and leaves the reference unresolved. Not landed on carve-js main (`3ca6d8c`) either; clears when the pin moves past the ports (carve-js#1610, carve-php#1879, carve-rs#1532).
447-the-host-does-not-change-which-column-a-definition-reaches-15  carve#1918 pins that a REPEATED label is a definition wherever it lands, so PART 9R's LAST definition wins picks the second. The pinned build publishes the second and resolves to the first. Not landed on carve-js main (`3ca6d8c`) either; clears when the pin moves past the ports (carve-js#1610, carve-php#1879, carve-rs#1532).
448-a-marker-folds-into-a-quote-below-it  carve#1905 pins that an unmarked marker line at an ENCLOSING item's content column folds into the quote below it, as lazy paragraph text in that quote's innermost open paragraph. The pinned build makes it a sibling item INSIDE the quote's list, which follows neither clause: the line carries no `>`, so it is in no quote under any reading. Not landed on carve-js main (`4627270`) either; clears when the pin moves past the ports (carve-js#1615, carve-php#1882, carve-rs#1537).
448-a-marker-folds-into-a-quote-below-it-2  carve#1905 pins the MARKER-LEAD spelling of the same geometry, where the quote sits on the item's lead line rather than below it. The pinned build makes it a sibling item INSIDE the quote's list, which follows neither clause: the line carries no `>`, so it is in no quote under any reading. Not landed on carve-js main (`4627270`) either; clears when the pin moves past the ports (carve-js#1615, carve-php#1882, carve-rs#1537).
448-a-marker-folds-into-a-quote-below-it-3  carve#1905 pins that the fold keeps the quote open, so intervening prose does not escape it and carries the marker under it in as well. The pinned build makes it a sibling item INSIDE the quote's list, which follows neither clause: the line carries no `>`, so it is in no quote under any reading. Not landed on carve-js main (`4627270`) either; clears when the pin moves past the ports (carve-js#1615, carve-php#1882, carve-rs#1537).
451-a-container-in-a-host-body-owns-a-line-past-its-own-content-column  carve#1937 pins that a definition written past an inner container content column is consumed under some hosts and not others; the pinned build reads consumption differently and renders the line as text while still resolving the reference (carve-js#1637). Clears when the pin moves past the fix.
451-a-container-in-a-host-body-owns-a-line-past-its-own-content-column-2  carve#1937 pins that a definition written past an inner container content column is consumed under some hosts and not others; the pinned build reads consumption differently and renders the line as text while still resolving the reference (carve-js#1637). Clears when the pin moves past the fix.
451-a-container-in-a-host-body-owns-a-line-past-its-own-content-column-3  carve#1937 pins that a definition written past an inner container content column is consumed under some hosts and not others; the pinned build reads consumption differently and renders the line as text while still resolving the reference (carve-js#1637). Clears when the pin moves past the fix.
451-a-container-in-a-host-body-owns-a-line-past-its-own-content-column-5  carve#1937 pins that a definition written past an inner container content column is consumed under some hosts and not others; the pinned build reads consumption differently and renders the line as text while still resolving the reference (carve-js#1637). Clears when the pin moves past the fix.
451-a-container-in-a-host-body-owns-a-line-past-its-own-content-column-6  carve#1937 pins that a definition written past an inner container content column is consumed under some hosts and not others; the pinned build reads consumption differently and renders the line as text while still resolving the reference (carve-js#1637). Clears when the pin moves past the fix.
451-a-container-in-a-host-body-owns-a-line-past-its-own-content-column-9  carve#1937 pins a lazy run in a quote swallowing a closer that has nothing to close; the pinned build ends the paragraph before it and emits the line as a block (carve-js#1637). Clears when the pin moves past the fix.
