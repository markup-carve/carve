# Corpus documents the pinned build READS correctly and WRITES wrongly.
#
# The pin-drift file next to this one is about the first render; this one is
# about the writer. A slug here means: the initial HTML matches the corpus, and
# re-reading the writer's own output does not.
#
# Delete a line the moment the pin moves past its fix - the header of
# engine-pin-drift.txt says the same thing for the same reason.
#
# Format: <slug><space><space><reason>
344-a-comment-only-line-in-a-line-block-is-removed-before-any-inline-run-2  carve#1333: a verse comment is already a `comment` node in the pin, so the canonical form is the source itself; the pin instead re-emits the marker with a LEADING SPACE, which PART 9 section 23 makes ordinary verse text, so the comment comes back visible.
344-a-comment-only-line-in-a-line-block-is-removed-before-any-inline-run-4  carve#1333: the same leading space, on the shape where the comment ends the stanza.
345-a-line-block-s-hard-break-keeps-its-backslash  carve#1334: the pin spells every line-block hard break as a bare newline, so it drops the backslash and PART 2 then strips the space the backslash was holding interior. PART 11 section 7c is the fix.
345-a-line-block-s-hard-break-keeps-its-backslash-2  carve#1334: the same writer rule, with a worse loss - the backslash-only line comes back as a BLANK line, which ends the stanza, so one stanza is returned as two.
346-a-line-block-s-last-body-line-keeps-its-backslash  carve#1334 as amended: the pin reads the trailing `hard_break` correctly and writes it as a BARE NEWLINE, which at a stanza's end is the closer's newline and not a boundary, so the break is dropped and the stanza gains a blank line. PART 11 section 7c's LAST BODY LINE consequence is the fix.
346-a-line-block-s-last-body-line-keeps-its-backslash-2  the same rule on the shape the old two-case list argued away: a TRAILING run of two or more columns needs no backslash inside a stanza, and at the stanza's end it is the break rather than the spaces that is lost.
346-a-line-block-s-last-body-line-keeps-its-backslash-3  the pin writes every inline comment with an unconditional leading separator space, which in verse is CONTENT, so the empty comment line comes back as ordinary text and the empty verse line is lost. Fixed in the engine at markup-carve/carve-js#1172; the fixture is here for the exemption, which the pin reaches for the opposite reason - it has no PART 11 section 7c at all.
346-a-line-block-s-last-body-line-keeps-its-backslash-4  the same rule plus the boundary question: the pin drops the LONE trailing space that the author's backslash was holding interior, and indents the comment line below it by a space, which in verse is content.
348-a-closed-inline-construct-spanning-a-verse-boundary-4  carve#1351: the pin READS this correctly - the backslash gives it the break - and its writer then spells the `hard_break` as a bare newline under PART 11 section 7c, which is a content change only because the pin does not harden the boundary inside the construct. Section 7c rests on the line block hardening every line boundary OF ITS OWN ACCORD, so this round trip is the writer half of the same reading and settles with it.
