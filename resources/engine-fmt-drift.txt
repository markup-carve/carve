# Corpus documents the pinned build READS correctly and WRITES wrongly.
#
# The pin-drift file next to this one is about the first render; this one is
# about the writer. A slug here means: the initial HTML matches the corpus, and
# re-reading the writer's own output does not.
#
# "RE-READING" IS DONE BY TWO READERS. The pin can fail to read its own output
# back, and the ORACLE can fail to read the pin's output back while the pin
# reads it fine - which is what a spec rule the pin has not shipped looks like
# from the writer side. Both belong here; corpus-fmt-cross-read.test.mjs has
# always consulted this file for the second kind (carve#1450).
#
# Delete a line the moment the pin moves past its fix - the header of
# engine-pin-drift.txt says the same thing for the same reason.
#
# Format: <slug><space><space><reason>
227-a-definition-inside-a-definition-list-dd-is-collected-and-the-entry-keeps-no-trace  the pin has not shipped the one-space canonical definition separator (carve#1757): it still writes `:  ` and leaves the body's continuations at the wider column.
227-a-definition-inside-a-definition-list-dd-is-collected-and-the-entry-keeps-no-trace-2  the pin has not shipped the one-space canonical definition separator (carve#1757): it still writes `:  ` and leaves the body's continuations at the wider column.
279-a-boundary-line-inside-an-open-fence-does-not-end-the-container-3  the pin has not shipped the one-space canonical definition separator (carve#1757): it still writes `:  ` and leaves the body's continuations at the wider column.
407-one-consumed-boolean-spells-the-looseness-no-blank-line-can-2  the pin has not shipped the one-space canonical definition separator (carve#1757): it still writes `:  ` and leaves the body's continuations at the wider column.
