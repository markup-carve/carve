# Corpus documents the pinned build READS correctly and WRITES wrongly.
#
# The pin-drift file next to this one is about the first render; this one is
# about the writer. A slug here means: the initial HTML matches the corpus, and
# re-reading the writer's own output does not.
#
# "RE-READING" IS DONE BY TWO READERS. The pin can fail to read its own output
# back, and the ORACLE can fail to read the pin's output back while the pin
# reads it fine - which is what a spec rule the pin has not shipped looks like
# from the writer side. Both belong here; corpus-fmt-cross-read.test.mjs has
# always consulted this file for the second kind (carve#1450).
#
# Delete a line the moment the pin moves past its fix - the header of
# engine-pin-drift.txt says the same thing for the same reason.
#
# Format: <slug><space><space><reason>
05-lists-25  The pinned JS build writes a tight item's two sibling sub-lists at the item's MARKER column, so it emits ONE blank line where PART 11 §10i requires three and the sub-lists merge back; remove when the pin advances past markup-carve/carve-js#1299's fix.
05-lists-26  The pinned JS build writes a tight item's two sibling sub-lists at the item's MARKER column, so it emits ONE blank line where PART 11 §10i requires three and the sub-lists merge back; remove when the pin advances past markup-carve/carve-js#1299's fix.
05-lists-27  The pinned JS build writes a tight item's two sibling sub-lists at the item's MARKER column, so it emits ONE blank line where PART 11 §10i requires three and the sub-lists merge back; remove when the pin advances past markup-carve/carve-js#1299's fix.
05-lists-28  The pinned JS build writes a tight item's two sibling sub-lists at the item's MARKER column, so it emits ONE blank line where PART 11 §10i requires three and the sub-lists merge back; remove when the pin advances past markup-carve/carve-js#1299's fix.
395-a-longer-run-at-a-list-boundary-is-written-as-exactly-three-blank-lines  The pinned JS build predates PART 9 §11 N1a, so it writes ONE blank line where PART 11 §10i requires three; remove when the pin advances past it.
396-an-idle-escape-does-not-spread-from-the-block-that-needed-one  The pinned JS build escalates escaping to the WHOLE document, so it writes `plain \(b\) text` where PART 11 §2b confines the escalation to the block that failed; remove when the pin advances past markup-carve/carve-js#1286's fix.
