# Converter-corpus cases an ENGINE is known to diverge on.
#
# `npm run compare:impls -- --corpus=convert` converts every case in
# tests/corpus-convert/ with each engine that implements the source format,
# renders every produced document through carve-js, and compares that render
# against the case's expected.html. A mismatch is not automatically wrong:
# when a converter rule is pinned here before an engine ships it, the corpus
# is ahead of that engine until its fix lands. That window is normal, and -
# exactly as with resources/engine-pin-drift.txt - it is DECLARED rather than
# tolerated.
#
# The runner fails (under --fail-on-diff) when the real set differs in EITHER
# direction:
#
#   - an (engine, case) pair mismatches and is not listed  -> new converter
#     drift, or a wrong expectation. Red immediately, which is the point.
#   - a pair is listed and now matches  -> the engine fix landed. Delete the
#     line in the same commit that proves it.
#
# Engine names are the runner's: rust, js, php. An engine with NO importer for
# a format does not belong here - that is a declared capability gap, and it
# lives in scripts/lib/converter-formats.mjs instead.
#
# Format: <engine>/<case-slug><space><space><reason>

# PART 11 section 1b, ruled at carve#1325: a producer that flattens block
# content into an inline slot emits a separator at every boundary between two
# former sibling blocks.
#
# Re-measured 2026-08-18 by converting all four inputs with each engine, built
# from a clone made for this run: carve-rs `3f8bde7b`, carve-js `099db2cc`,
# carve-php `925f7dcf`. carve-rs emits the separator on every row since
# markup-carve/carve-rs#1094, so its four lines are gone; carve-js and carve-php
# still join the blocks with nothing.
#
#     figcaption content                          rs        js and php
#     <p>one</p><p>two</p>                        one two   onetwo
#     <p><strong>a</strong></p>
#     <p><strong>b</strong></p>                   *a* *b*   *a**b*
#     <p><code>a</code></p>
#     <p><code>b</code></p>                       `a` `b`   `a``b`
#     <p>a</p><p></p><p>b</p>                     a b       ab
#
# The last row is the one that says the rule is over CONTRIBUTED TOKENS: the
# empty block is not a side, so the emitted caption carries one separator and
# not two. Each remaining line goes in the commit that lands the fix for that
# engine.
#
# AND ALL THREE LANDED. Measured 2026-08-18 at the release freeze: carve-rs
# first (markup-carve/carve-rs#1094), then carve-js and carve-php, so every
# js/ and php/ line for cases 29 through 32 reported STALE and is deleted here.
#
# THIS FILE IS NOW EMPTY OF DECLARATIONS, and that is the intended end state
# rather than a missing section: no engine is known to diverge on any
# converter case. The window PART 11 s1b opened - corpus ahead of every engine
# - is closed. A new declaration goes below, one line per (engine, case), and
# is deleted by the commit that proves the engine matches.
#
# A ledger and an unwired gate look identical from outside, so read the
# converter summary rather than this file: `npm run compare:convert` prints a
# convert_pass count per engine, and it is that count, not the contents here,
# that says the cases are being converted and compared.

# PART 9 section 18, ruled at carve#1514: an importer meeting
# <div class="math display"> emits the CORE $$ form, never the ``` math ```
# extension fence, because the fence degrades to a language-math code block
# wherever the extension is not loaded.
#
# Measured 2026-08-22, each engine built from a clone made for the run:
# carve-js 906357c writes the core form already (markup-carve/carve-js#1295);
# carve-php 3a06c99 writes the fence (markup-carve/carve-php#1546 chose it and
# now changes to match); carve-rs 3250454 leaves the div generic, deliberately
# unimplemented under markup-carve/carve-rs#1222 pending this ruling.
#
# Each line goes in the commit that lands the fix for that engine.
#
# AND BOTH LANDED, on the same day the clause did: carve-php took the core form
# in markup-carve/carve-php#1561, and carve-rs rewrote the placeholder test that
# held its gap open in markup-carve/carve-rs#1236. So both lines reported STALE
# and are deleted here.
#
# The deletion is a MEASUREMENT, not an inference from the merges. Run against
# the three merged mains - carve-js 93d469d, carve-php fc568db, carve-rs c7cee5a
# - `npm run compare:convert` reported case 33 passing for every engine and
# named exactly these two lines as the reason it failed:
#
#     rust: convert_pass=31/31 declared_drift=0 mismatch=0 error=0 skipped=2
#     js:   convert_pass=31/31 declared_drift=0 mismatch=0 error=0 skipped=2
#     php:  convert_pass=33/33 declared_drift=0 mismatch=0 error=0 skipped=0
#     cross_convert_diffs=0
#
# That is the shape a stale ledger line has, and the reason the gate checks BOTH
# directions: an undeclared divergence and a declaration nothing diverges on are
# the same defect, and only one of them announces itself.
#
# THIS FILE IS EMPTY OF DECLARATIONS AGAIN, which is the intended end state.

# PART 11 import canonicalization, ruled at carve#2171: an empty BBCode quote
# survives as an empty block quote and is written as `>`, without trailing
# whitespace. carve-php already writes that form. The JavaScript importer keeps
# the quote but writes `> `, while the Rust importer drops it.
js/46-bbcode-empty-quotes-survive  markup-carve/carve#2171: remove the trailing space from an empty quote marker
rust/46-bbcode-empty-quotes-survive  markup-carve/carve#2171: preserve an empty quote as `>`


# Full HTML-import fixture sweep added by carve#1600, measured 2026-08-24 on
# clean current mains: carve-rs 9cf16d05, carve-js 05e1cc27, carve-php 06c27aaa.
# Each entry is removed when that engine reproduces the normative fixture.
#
# FIVE OF THE EIGHT LANDED, and the ledger's stale half is what said so. Measured
# 2026-08-24 in AST conformance run 32683876950, at carve 4f911379, which builds
# every engine from a fresh checkout of its own default branch:
#
#     rust: convert_pass=60/61 declared_drift=1 mismatch=0 error=0 skipped=2
#     js:   convert_pass=61/63 declared_drift=1 mismatch=1 error=0 skipped=0
#     php:  convert_pass=61/63 declared_drift=1 mismatch=1 error=0 skipped=0
#
# and it named exactly these five as STALE, one failure line each:
#
#     rust/html-import--empty-definition-description           carve-rs#1319
#     rust/html-import--empty-definition-description-not-last  carve-rs#1319
#     php/html-import--empty-definition-description-not-last   carve-php#1646
#     rust/html-import--endnotes-section-not-last              carve-rs#1318
#     rust/html-import--note-reference-in-a-span               carve-rs#1317
#
# The three derived-endnotes-section lines were NOT stale in that run: each
# engine's declared_drift=1 above is that line being used.
#
# AND THEN ALL THREE LANDED, together. carve#1612 gave the one-item loose list a
# consumed `{loose}` boolean, and every importer now preserves the derived
# endnotes list's looseness. Measured in AST conformance run 32750910101, at
# carve 6044cba0, which builds every engine from a fresh checkout of its own
# default branch:
#
#     rust: convert_pass=69/69 declared_drift=0 mismatch=0 error=0 skipped=2
#     js:   convert_pass=71/71 declared_drift=0 mismatch=0 error=0 skipped=0
#     php:  convert_pass=71/71 declared_drift=0 mismatch=0 error=0 skipped=0
#     cross_convert_diffs=0
#
# declared_drift=0 on every engine, with these three still declared, is the
# stale shape: the run named each of them and failed on nothing else.
#
# THIS FILE IS EMPTY OF DECLARATIONS AGAIN, which is the intended end state.

# The Markdown importer rulings carve-js shipped in markup-carve/carve-js#1922
# through #1948, plus the `1.`-only item interruption of carve-js#1940: import
# writes what `carve fmt` writes and reads what cmark-gfm reads. Twenty lines
# declared carve-php behind on them, measured against carve-php `4b8cce25`.
#
# ALL TWENTY WERE STALE WITHIN THE DAY. carve-php ported the set in carve-php
# #2266, #2269 and #2275, merged between 09:14 and 12:00 UTC on 2026-09-24 -
# hours after the measurement at `4b8cce25` (01:13 UTC) and after the commit
# that declared them. Two AST conformance runs four and a half hours apart,
# [35999665301](https://github.com/markup-carve/carve/actions/runs/35999665301)
# and
# [36029258478](https://github.com/markup-carve/carve/actions/runs/36029258478),
# both score carve-php at `convert_pass=115/115 declared_drift=0` and name the
# same twenty as stale.
#
# Worth keeping in mind before declaring an engine behind: this file is read by
# a job that builds every engine from its own main, so a window measured at a
# commit closes without anything in this repo moving.
#
# THIS FILE IS EMPTY OF MARKDOWN-IMPORTER DECLARATIONS AGAIN.

# PART 9 section 14, ruled at carve#2273: a Markdown label that is both a GFM
# task checkbox and a defined link reference label is the CHECKBOX, because the
# importers answer to cmark-gfm 0.29.0.gfm.13 (carve#2187) rather than to
# GitHub's own reader.
#
# carve-rs takes the reference link. Measured at `8beadb6` in
# markup-carve/carve-rs#1888, which also corrects the ruling's premise that
# carve-rs was already right here: the reading arrived in carve-rs#1875, merged
# eight minutes before the ruling landed.
#
# carve-php is NOT declared, deliberately. The only measurement of it is the
# ruling's, at `d0a71410`, and carve-php ported a large Markdown-importer set on
# 2026-09-24 (carve-php#2266, #2269, #2275) - the same shape of stale window the
# note above this one describes. An undeclared divergence is red immediately,
# which is what names the real set here rather than a guess.
rust/70-markdown-a-task-label-that-is-also-a-reference-definition  markup-carve/carve-rs#1888: read the checkbox and leave the definition unused
