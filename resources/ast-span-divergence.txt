# Node types the three engines SPAN differently.
#
# `npm run ast:check` compares every corpus document's AST across carve-js,
# carve-rs and carve-php. This ledger makes a newly introduced position presence
# or extent disagreement fail conformance instead of becoming an unnoticed
# baseline.
#
# Format, one row per diverging node type:
#
#   <type> (presence|extent)  <document-count>
#
# The declaration is exact in three directions, and `reconcileSpans`
# (scripts/spec/ast-spans.mjs) reports each one:
#
#   - a type diverges and is not listed     -> NEW, fails
#   - a listed type no longer diverges      -> AGREED, delete the row, fails
#                                              until you do
#   - a listed type's DOCUMENT COUNT moves  -> COUNT, the divergence spread or
#                                              shrank, fails
#
# The count is what keeps the second and third honest. A list of document names
# would churn on every corpus insertion; a count describes the same fact and
# still fails when the fact changes.
#
# A ROW KEY IS A TYPE, NOT A DEFECT. One key can carry faults owned by different
# issues in different engines, and no number here says so - so give every row a
# `markup-carve/...#NNNN` reference in a trailing comment. That reference is what
# makes a short row safe to read; without it a row is a rumor.
#
# A row does not say WHICH engine is wrong: this panel has the trees and not the
# source. `npm run ast:check` prints the reasoning, and PART 12 §4 names the side
# only where `checkOpeningMarkup` / `checkStopsAtChildren` in
# scripts/spec/ast-positions.mjs can see it.
#
#
# WHICH ISSUE OWNS WHICH ROW, as of 2026-09-23. A row is a TYPE and an issue is
# a DEFECT, so the mapping is not one to one:
#
#   markup-carve/carve-rs#1832   an inline code span opened on an indented
#     continuation line starts in the indentation instead of at its backtick.
#     `code (extent)` and `soft_break (extent)` - the indent is charged to one
#     of the two, so the pair moves together.
#   markup-carve/carve-rs#1833   a description body's fenced code block is
#     published with no position, and the containers around it stop at their
#     last placed child. `code_block (presence)`, `definition_list (extent)`
#     and `definition_description (extent)`.
#   markup-carve/carve-php#2250  an unresolved reference's literal caption text
#     and its soft break carry no position. `text (presence)`,
#     `soft_break (presence)`, `paragraph (extent)`, and one of the four
#     documents under `list (extent)` and `list_item (extent)`.
#   markup-carve/carve-php#2251  a code block ended by the item that holds it
#     spans only its opening fence line. `code_block (extent)`, and the other
#     three documents under those two list rows.
#
# THE TWO LIST ROWS ARE SHARED on purpose. A container is narrowed by whichever
# child below it is misplaced, so closing either issue lowers those counts
# without emptying them, and a reader who finds only one issue on them has half
# the story.
#
# HOW A MEASUREMENT IS RECORDED. In the pull request that took it, not here. This
# file states what is declared NOW; the run that established it, the engine
# commits it was taken at, and what moved since are the PR's job, where they are
# immutable and nobody has to scroll past them to reach the rows.

code (extent) 5
soft_break (extent) 5
code_block (presence) 4
definition_list (extent) 3
definition_description (extent) 3
text (presence) 4
soft_break (presence) 4
paragraph (extent) 4
code_block (extent) 3
list (extent) 4
list_item (extent) 4
